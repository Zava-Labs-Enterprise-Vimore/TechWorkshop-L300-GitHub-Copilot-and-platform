# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ZavaStorefront
metadata:
  template: ZavaStorefront@0.0.1

infra:
  provider: bicep
  path: infra
  module: main

services:
  web:
    project: ./src
    language: csharp
    host: appservice
    docker:
      path: ./Dockerfile
      context: ./src

hooks:
  postprovision:
    windows:
      shell: pwsh
      run: |
        Write-Host "Infrastructure provisioned successfully!"
        Write-Host "App Service: $(azd env get-value APP_SERVICE_NAME)"
        Write-Host "Container Registry: $(azd env get-value AZURE_CONTAINER_REGISTRY_NAME)"
    posix:
      shell: sh
      run: |
        echo "Infrastructure provisioned successfully!"
        echo "App Service: $(azd env get-value APP_SERVICE_NAME)"
        echo "Container Registry: $(azd env get-value AZURE_CONTAINER_REGISTRY_NAME)"
